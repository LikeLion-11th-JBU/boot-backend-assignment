<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "http://mabatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.SeungIL.mybatis.mapper.BoardMapper">
    <insert
            id="createBoard"
            useGeneratedKeys="true"
            keyProperty="id"
            parameterType="dev.SeungIL.mybatis.dto.BoardDto">
        insert into board(name) values (#{name})
    </insert>
</mapper>